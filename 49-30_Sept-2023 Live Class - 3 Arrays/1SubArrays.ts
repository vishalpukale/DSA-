let arr = [1, 2, 3, 4];

function subArrays(arr){

    for(let sp = 0; sp < arr.length; sp++){
        let sum = arr[sp];
        for(let ep = sp; ep < arr.length; ep++){
            
        }
    }
    
}
subArrays(arr);